import { makeStyles } from "@material-ui/core/styles";

export default makeStyles((theme) => ({
  pageTopBar: {
    display: "flex",
    justifyContent: "right",
  },
  marginTop: {},
  emptyState: {
    color: "#ccc",
    marginTop: "10vw",
    textAlign: "center",
  },
}));
